spring:
  application:
    name: gulimall-coupon
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        namespace: b5d1f293-6c56-420f-bd58-45b71884a11b
        group: dev
        extension-configs[0]:
          data-id: datasource.yaml
          group: dev   # 默认为DEFAULT_GROUP
          refresh: true   # 是否动态刷新，默认为false
        extension-configs[1]:
          data-id: mybatis.yaml
          group: dev
          refresh: true
        extension-configs[2]:
          data-id: other.yaml
          group: dev
          refresh: true